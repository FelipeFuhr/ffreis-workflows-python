name: Python Lint

on:
  workflow_call:
    inputs:
      python-version:
        description: "Python version to use"
        type: string
        default: "3.13"
      working-directory:
        description: "Directory to run steps in"
        type: string
        default: "."
      source-dirs:
        description: "Space-separated dirs to check with ruff"
        type: string
        default: "src tests"
      mypy-dirs:
        description: "Space-separated dirs to check with mypy"
        type: string
        default: "src"
      uv-extras:
        description: "uv extras to install (e.g. dev)"
        type: string
        default: "dev"

permissions:
  contents: read

jobs:
  lint:
    name: Ruff + Mypy
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Set up uv
        uses: astral-sh/setup-uv@eac588ad8def6316056a12d4907a9d4d84ff7a3b
        with:
          enable-cache: true

      - name: Assert uv.lock exists
        run: |
          set -euo pipefail
          test -s uv.lock || (echo "uv.lock missing — run uv lock"; exit 1)

      - name: Install deps
        run: |
          set -euo pipefail
          uv sync --frozen --extra ${{ inputs.uv-extras }}

      - name: Ruff check
        run: |
          set -euo pipefail
          uv run ruff check ${{ inputs.source-dirs }}

      - name: Mypy
        run: |
          set -euo pipefail
          uv run mypy ${{ inputs.mypy-dirs }}
