FROM python:3.13-slim

WORKDIR /app

RUN pip install --no-cache-dir uv

COPY pyproject.toml uv.lock ./
COPY docs/ docs/
RUN uv sync --frozen --no-dev

COPY src/ src/

ENTRYPOINT ["uv", "run", "hello"]
CMD ["world"]
